name: Automate Testing
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: docker build -t dzung-min/react-test-vite -f Dockerfile.dev .
      - run: docker run -e CI=true dzung-min/react-test-vite npm test